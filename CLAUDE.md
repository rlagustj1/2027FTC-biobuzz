# 공학 바이브 코딩 가이드 (FTC 제어공학 적용용)

이 프로젝트는 FTC(FIRST Tech Challenge) 로봇 제어 코드(모션 프로파일링, PIDF, 오도메트리 등)를 다룬다. 웹/앱 코딩과 달리 실기 검증이 필요하고 실패 비용이 높으므로, 코드를 작성/수정할 때 반드시 아래 원칙을 따를 것.

## 핵심 전제: 웹 코딩과 공학 코딩은 검증 방식이 다르다

| 구분 | 웹 바이브 코딩 | 공학 바이브 코딩 |
|---|---|---|
| 검증 방법 | 화면 보고 바로 판단 가능 | 실제로 로봇을 굴려봐야 판단 가능 |
| 실패 비용 | 낮음 (새로고침하면 됨) | 높음 (하드웨어 손상, 안전 문제 가능) |
| 파라미터 | AI가 임의로 채워도 무방 (색상, 폰트 등) | 반드시 실측/튜닝 필요 (vmax, amax, kP/kI/kD) |
| 디버깅 단위 | 전체 페이지 단위로 봐도 됨 | 반드시 작은 단위로 쪼개서 검증 |

## 규칙 1: 파라미터는 절대 "적당히" 채우지 않는다

다음 값들은 코드에 **플레이스홀더로 명확히 표시**하고, 절대 임의의 숫자를 최종값처럼 채워 넣지 않는다:

- `VMAX`, `AMAX`, `JMAX` (모션 프로파일 파라미터)
- `kP`, `kI`, `kD`, `kF` (PIDF 게인)
- 모터 free speed, 기어비, 바퀴 반지름 등 하드웨어 스펙

이런 값을 코드에 넣을 때는 반드시 아래 형식으로 주석을 남긴다:

```java
// TODO: 실측 필요 — goBILDA 5202 free speed 기준 이론값이므로
// 실제 로봇에 올려서 배터리 전압/부하 상태에서 재측정할 것
double VMAX = 2800; // 틱/초 (임시값, free speed의 약 80%로 추정)
```

AI가 "일반적으로 이 정도 씁니다"라고 제안한 값은 반드시 **참고용**이며, 사용자가 실제 로봇에서 재측정하기 전까지 최종값으로 취급하지 않는다.

## 규칙 2: 단위를 항상 명시하고 검증한다

공학 코드에서 가장 흔한 버그는 단위 불일치다. 함수를 작성하거나 수정할 때마다 다음을 확인한다:

- 시간: 초(s) vs 밀리초(ms)
- 각도: 도(degree) vs 라디안(radian)
- 거리: 인치(inch) vs 센티미터(cm) vs 엔코더 틱(tick)

모든 함수 시그니처에 단위를 주석으로 명시한다:

```java
/**
 * @param targetPosition 목표 위치 (엔코더 틱 단위)
 * @param currentVelocity 현재 속도 (틱/초 단위)
 * @return 모터 출력 (-1.0 ~ 1.0)
 */
```

## 규칙 3: 작은 단위로 쪼개서 순차 검증한다

한 번에 전체 시스템을 완성하지 않는다. 아래 순서로 단계별로 작업하고, **각 단계마다 사용자에게 결과 확인을 요청**한다:

1. **프로파일 생성 함수만 먼저 작성** → Python 시뮬레이션으로 이론적인 v-t, x-t 그래프 출력 → 사용자가 모양 확인
2. **PIDF 컨트롤러만 별도로 작성 및 단위 테스트**
3. **프로파일 + PIDF 통합** (시뮬레이션 환경에서)
4. **Java(FTC SDK)로 이식** — 이 단계부터는 실제 로봇 필요
5. **실제 로봇에서 파라미터 튜닝** — 이 단계는 AI가 대신할 수 없음, 반드시 사용자가 직접 실기 테스트

각 단계를 건너뛰고 통합 코드를 한 번에 요청받아도, Claude Code는 단계별 쪼개기를 제안하고 사용자 동의 없이 4단계 이상을 한 번에 진행하지 않는다.

## 규칙 4: 시뮬레이션 우선, 실기는 나중

로직 검증은 반드시 Python(numpy/matplotlib)으로 먼저 진행한다:

- 사다리꼴/S-curve 프로파일의 위치·속도·가속도 그래프를 그려서 이론적으로 맞는 모양인지 확인
- 목표 거리와 사다리꼴 아래 면적(적분값)이 일치하는지 수치적으로 검증하는 assert 코드 포함

시뮬레이션에서 검증되지 않은 로직은 Java(FTC SDK) 코드로 이식하지 않는다.

## 규칙 5: 짧은 거리(삼각형 프로파일) 분기 처리 필수

목표 거리가 짧아 vmax에 도달하기 전에 감속을 시작해야 하는 경우를 반드시 별도로 처리한다. 이 분기를 빠뜨리면 짧은 이동 명령에서 프로파일이 깨진다. 코드에 다음 조건 분기가 포함되어야 한다:

```
if (가속+감속에 필요한 최소 거리 > 목표 거리):
    → 삼각형 프로파일로 전환 (실제 도달 최고속도는 vmax보다 낮음)
else:
    → 표준 사다리꼴 프로파일
```

## 규칙 6: 원리를 항상 설명하도록 요구한다

코드를 제시할 때마다 "왜 이렇게 짰는지"를 함께 설명한다. 특히:

- PIDF에서 F(피드포워드)항이 왜 그 위치(가산 순서)에 들어가는지
- 프로파일 재계산 주기가 왜 그 값인지(제어 루프 주기와의 관계)

이는 세특 탐구 산출물로서 사용자가 원리를 이해하고 있음을 보이기 위한 필수 과정이며, 코드를 실행만 하고 원리를 모르는 상태로 넘어가지 않기 위함이다.

## 규칙 7: 실측 후 이론-실측 비교 그래프를 남긴다

실기 테스트 후에는 엔코더 로그 데이터를 저장하고, 이론 프로파일(목표 v-t)과 실측 프로파일(실제 v-t)을 겹쳐 그린 비교 그래프를 생성한다. 오차가 발생한 지점에 대해 다음 원인 중 어느 것인지 분석한다:

- 모터 토크/속도 한계(saturation)
- 배터리 전압 강하
- 바퀴 슬립
- 제어 루프 지연

## 우선순위 (시즌 중 시간이 부족할 경우)

1. PIDF 기본 제어 (필수)
2. 오도메트리 (프로파일링의 전제조건)
3. 모션 프로파일링 — 사다리꼴 (가성비 최고, S-curve는 후순위)
4. LQR, 자코비안 등 — 시즌 중 실전 적용보다 세특 이론 탐구용으로 우선 남겨둠

## 체크리스트 (코드 완성 전 최종 확인)

- [ ] VMAX, AMAX 등 파라미터에 "실측 필요" 주석이 달려 있는가
- [ ] 모든 함수의 단위가 주석으로 명시되어 있는가
- [ ] 짧은 거리(삼각형 프로파일) 분기가 처리되어 있는가
- [ ] 시뮬레이션 검증을 거쳤는가
- [ ] 실기 테스트 후 이론-실측 비교 그래프 계획이 있는가
- [ ] 원리(왜 이렇게 짰는지)를 설명할 수 있는가

## 프로젝트 기술 스택

- **공식 FtcRobotController 구조** (SDK 11.0) + FTCLib 2.1.1
- 팀 코드 위치: `TeamCode/src/main/java/org/firstinspires/ftc/teamcode/`
- 배포: `./gradlew :TeamCode:installDebug` (USB), RC 앱 = `com.qualcomm.ftcrobotcontroller`
- 시뮬레이션: Python (numpy/matplotlib), 검증 후에만 Java로 이식
- 외부 PC: `pc/` (통신 클라이언트, 웹 대시보드, AI 오케스트레이터)

## 규칙 8: FTC 파일은 정해진 패키지 구조를 따른다

새 FTC(Java) 파일을 만들 때는 반드시 아래 패키지 구조에 맞춰 배치한다. 파일을
`teamcode/` 루트에 흩뿌리지 않는다. (작년 팀 레포 컨벤션과 동일)

```
TeamCode/src/main/java/org/firstinspires/ftc/teamcode/
├── opmodes/            OpMode만 모음 (@TeleOp/@Autonomous 붙은 클래스)
│   ├── TeleOp/           수동 조종 OpMode
│   ├── auto/             자율주행 OpMode
│   └── tests/            하드웨어/기능 테스트용 OpMode
├── subsystems/         드라이브트레인·팔 등 하드웨어 서브시스템 (FTCLib SubsystemBase)
├── commands/           동작 단위 Command (FTCLib CommandBase)
├── localization/       오도메트리·야코비안 등 위치추정/기구학
├── comm/               외부 PC 통신 (소켓 서버 등)
└── Utils/              공용 상수·헬퍼
```

원칙:
- **OpMode는 반드시 `opmodes/` 하위**에 두고, 종류별(TeleOp/auto/tests)로 분류한다.
- 로직(subsystem/command/localization)과 진입점(OpMode)을 섞지 않는다.
- 새 폴더가 필요하면 이 구조와 일관되게 소문자 패키지명으로 추가한다.
- 파일을 옮기면 `package` 선언과 import를 반드시 함께 수정하고 빌드로 검증한다.
